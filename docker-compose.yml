version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: toxscreen-postgres
    environment:
      POSTGRES_DB: toxscreen
      POSTGRES_USER: toxscreen
      POSTGRES_PASSWORD: toxscreen_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U toxscreen"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: toxscreen-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: ./api
    container_name: toxscreen-api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://toxscreen:toxscreen_password@postgres:5432/toxscreen
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: development
      DEBUG: "true"
    volumes:
      - ./api:/app
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

volumes:
  postgres_data:
  redis_data:
